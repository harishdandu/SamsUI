{
  "name": "edtechschoolmanagement-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd backend && npm install && cd .. && cd frontend && npm install && npm run build && cd .. && rm -rf backend/public && cp -r frontend/dist backend/public && mkdir -p public && touch public/.gitkeep",
    "start": "node backend/index.js",
    "dev": "concurrently \"cd frontend && npm run dev\" \"node backend/index.js\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.3.2",
    "nodemailer": "^6.9.13",
    "openai": "^4.35.0"
  }
}
