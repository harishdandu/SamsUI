{
  "name": "edtechschoolmanagement-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf backend/public && cp -r frontend/dist backend/public && mkdir -p public && touch public/.gitkeep",
    "start": "node backend/index.js",
    "dev": "concurrently \"cd frontend && npm run dev\" \"node backend/index.js\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "mongoose": "^9.6.1"
  }
}
